import { Injectable } from '@angular/core';
import { BehaviorSubject } from 'rxjs';
@Injectable({
  providedIn: 'root'
})

export class SharedService {

  //Declare variables
  //private var = new BehaviorSubject(true);

  //Observables

  //currentVarStatus = this.var.asObservable();
  constructor() {}


  //set(status: boolean) {
  //  this.var.next(status);
  //}
}
